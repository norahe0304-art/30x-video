# lib/
> L2 | 父级: scaffold/src/AGENTS.md

成员清单
fonts.ts: 全局字体加载器，供 index.ts 预热字体资源。
modes.ts: product-evidence / editorial 的 mode blueprint 与 scene 结构。
utils.ts: cn() 等轻量样式工具。

法则: 只放共享的、无副作用的基础能力；可被主线程 orchestrator 直接读取。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
